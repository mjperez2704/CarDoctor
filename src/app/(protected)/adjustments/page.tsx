import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function AdjustmentsPage() {
  return (
      <Card>
        <CardHeader>
          <CardTitle>Ajustes de Inventario</CardTitle>
          <CardDescription>
            Gestiona los ajustes de inventario.
          </CardDescription>
        </CardHeader>
        <CardContent>
          <p>Próximamente: Módulo de ajustes de inventario.</p>
        </CardContent>
      </Card>
  );
}
